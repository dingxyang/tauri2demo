# Android APK 签名配置示例文件
# 复制此文件为 keystore.config 并填入你的签名信息

# 密钥库别名
ANDROID_KEY_ALIAS=your_key_alias

# 密钥库密码
ANDROID_KEY_PASSWORD=your_keystore_password

# 密钥库文件路径（绝对路径或相对于 backend 目录的路径）
ANDROID_KEYSTORE_PATH=/path/to/your/keystore.jks

# 注意：
# 1. keystore.config 文件已被添加到 .gitignore，不会被提交到版本控制
# 2. 请妥善保管你的密钥库文件和密码
# 3. 如果不配置签名信息，将生成未签名的 APK 文件
# 4. 此文件应该放在 backend/ 目录下
